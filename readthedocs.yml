version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .
    # - requirements: docs/requirements.txt  # uncomment if needed

sphinx:
  configuration: docs/conf.py

#python:
#  install:
#    - requirements: requirements.txt
#    - requirements: requirements-dev.txt
#    - method: pip
#      path: .

# Don't build any extra formats
formats: []
